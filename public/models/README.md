# Model assets

`demo-rigged.glb` is a deliberately simple development asset generated by
`npm run generate:demo-model`. Replace or supplement it with a prepared CLO /
Blender `.glb` while keeping model files browser-local.
